# Extra deps for the training pipeline (on top of the app's requirements.txt).
# Install on the rented GPU box, not necessarily locally:
#   pip install -r training/requirements-train.txt
torch>=2.3
transformers>=4.45
datasets>=2.20
peft>=0.13
trl>=0.11
accelerate>=0.34
mergekit>=0.0.5
pyyaml>=6.0
